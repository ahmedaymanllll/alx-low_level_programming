#include"main.h"

/**
 * Description: how to Write a function that prints the alphabet,
 *		in lowercase, followed by a new line.
 *
 * Return: 0 (success)
*/

int main(void)
{
	int ch;

	for (ch = 0; ch < 25; ch++)
		_putchar(ch);
	_putchar('\n');

	return (0);
}
