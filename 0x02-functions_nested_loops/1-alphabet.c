#include"main.h"

/**
 * main - Entry point
 *
 * Description: 
 *
 * Return: 0 (success)
*/

int main(void)
{
	char str[] = {a..z};
	int ch;

	for (ch = 0; ch < 25; ch++)
		a..z (str[ch]);
	a..z('\n')

	return (0);
}
