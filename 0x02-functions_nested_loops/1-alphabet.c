#include"main.h"

/**
 * print_alphabet - utilizes on the _putchar
 * 			function to print the alphabet a - z
*/

int main(void)
{
	int ch;

	for (ch = 0; ch < 25; ch++)
		_putchar(ch);
	_putchar('\n');

	return (0);
}
